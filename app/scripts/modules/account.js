(function (app) {
  'use strict';
  // module name must correspond to route name in routing.js
  app.modules.account = {
    init: init,
    cleanup: cleanup
  };

  function cleanup() {

  }

  function init() {

  }

})(window.clientSettingsApp);
