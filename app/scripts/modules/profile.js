(function (app) {
  'use strict';
  // module name must correspond to route name in routing.js
  app.modules.profile = {
    init: init,
    cleanup: cleanup
  };

  function cleanup() {

  }

  function init() {

  }

})(window.clientSettingsApp);
